# Next.js frontend for OmLang Compiler

This is a minimal Next.js (pages router) frontend that posts source to the backend /compile endpoint.

Environment Variables (set in Vercel):
- NEXT_PUBLIC_API_URL – full URL to the backend, e.g. https://om-backend.example.com

Run locally
- cd frontend
- npm install
- npm run dev

